# 高血脂风险分层预警与干预推荐系统 Web版 V1.0

## 项目简介

基于数模论文《**基于中西医融合信息的高血脂风险识别、分层预警与干预优化研究**》（队伍编号: CMC2604725），完整复现论文三大核心算法，实现为可运行的软件系统，适用于本科软件著作权申请。

- ✅ **Web单服务架构** - Flask 同时提供网页与 REST API，启动后直接访问浏览器使用
- ✅ **完整前后端分离代码** - 前端HTML+JS + 后端Python Flask API
- ✅ **100% 复现论文算法** - 三大核心模块完整实现，不修改论文逻辑
- ✅ **功能精简够用** - 满足本科软著标准，代码量充足，结构清晰

## 目录结构

```
hyperlipidemia-risk-system/
├── README.md                 # 项目说明（本文件）
├── requirements.txt          # Web版一键安装依赖
├── 软著申请说明.md            # 软著申请专用说明
├── docs/                     # 软著配套文档
│   ├── 用户使用手册.md
│   ├── 软件设计说明书.md
│   ├── 测试报告.md
│   └── 软著提交材料清单.md
├── backend/                  # 后端Python
│   ├── app.py                # Flask API入口
│   ├── requirements.txt      # Web运行依赖
│   ├── requirements-ml.txt   # 可选训练/SHAP分析依赖
│   ├── README.md             # 后端说明
│   ├── config/               # 配置文件
│   │   ├── risk_thresholds.json      # 论文提取的风险阈值
│   │   └── intervention_costs.json   # 干预成本参数
│   ├── models/               # 三大核心算法模块
│   │   ├── feature_identification.py  # 模块一: 关键风险因素识别
│   │   ├── risk_classification.py     # 模块二: 三级风险分层预警
│   │   └── intervention_optimization.py  # 模块三: 个体化干预优化
│   └── utils/                # 工具函数
│       └── data_loader.py
└── frontend/                 # 前端HTML+JS
    ├── index.html            # 主页面（四步骤流程）
    ├── about.html            # 关于页面
    └── README.md            # 前端说明
```

## 三大核心算法（完全对照论文）

| 论文问题 | 算法 | 对应代码文件 | 论文结论 |
|----------|------|--------------|----------|
| **问题一** | Lasso-Logistic + XGBoost-SHAP + 随机森林交叉验证 | `backend/models/feature_identification.py` | TG、TC、LDL-C、HDL-C、血尿酸是主要代谢主轴，痰湿质与BMI有交互放大效应 |
| **问题二** | CART决策树 + 中西医融合三级标签 | `backend/models/risk_classification.py` | 5折交叉验证准确率 **95.00%**，关键阈值: 痰湿≈60分、TG≈1.7、TC≈6.2、活动能力≈40 |
| **问题三** | 画像分型 + 约束组合优化 | `backend/models/intervention_optimization.py` | 按原题附表2、附表3匹配调理等级、活动强度、训练频率和6个月成本约束 |

## 快速开始

### 1. 安装依赖

```bash
pip install -r requirements.txt
```

### 2. 启动 Web 服务

```bash
cd backend
python app.py
```

服务启动在 `http://localhost:5000`。

### 3. 打开 Web 应用

浏览器访问：

```text
http://localhost:5000
```

同一个 Flask 服务会自动提供前端页面和 `/api/*` 后端接口，不再需要单独打开 `frontend/index.html`。

### 可选：训练型特征识别接口

普通风险评估不需要安装机器学习重型依赖。如需使用 `/api/identify` 重新执行 Lasso-Logistic、XGBoost-SHAP 与随机森林特征识别，额外安装：

```bash
pip install -r backend/requirements-ml.txt
```

## 功能流程

1. **信息录入** → 输入年龄、性别、BMI、血脂指标、痰湿积分、活动能力、预算
2. **快捷演示** → 内置低风险、中风险、高风险示例病例，一键填充
3. **风险计算** → 一键调用后端 API，执行风险分层、患者分型和干预优化
4. **风险结果** → 展示低/中/高风险分级、异常因素、患者分型和指标阈值对比图
5. **干预推荐** → 按原题约束给出调理等级、活动强度、每周频率、单次时长、成本拆分和预期痰湿积分下降
6. **报告管理** → 生成评估报告，支持复制和打印，便于软著截图和线下留档

## 患者分型策略（来自论文）

| 患者类型 | 特征 | 推荐策略 |
|----------|------|----------|
| **代谢异常主导型** | 血脂异常为主，体质和活动能力尚可 | 优先强化调理 |
| **肥胖并发型** | BMI超标 + 痰湿质 + 代谢异常 | 调理 + 运动联合干预 |
| **功能受限型** | 活动能力下降 + 高龄 + 多种并发症 | 低强度稳步改善方案 |

## 技术栈

| 层级 | 技术 | 说明 |
|------|------|------|
| 后端 | Python 3.8+ + Flask | 完整实现论文三大算法 |
| 机器学习 | Scikit-learn + XGBoost + SHAP | 可选安装，用于训练型特征识别接口 |
| 前端 | HTML5 + Bootstrap 5 + 原生JavaScript | 简单清爽，前后端分离 |
| API | RESTful JSON | 前后端完全分离，易于维护 |

## 软著申请

项目已补充软著申请配套材料：

- `软著申请说明.md`：软件基本信息、功能模块、算法说明和运行说明
- `docs/用户使用手册.md`：面向用户的安装、启动和操作说明
- `docs/软件设计说明书.md`：系统架构、模块划分、接口和算法设计
- `docs/测试报告.md`：核心功能测试用例和测试结论
- `docs/软著提交材料清单.md`：源代码、文档和申请表信息整理清单

提交前需要人工补全：

- 基本信息填写模板
- 著作权人姓名或单位
- 开发者信息
- 首次发表日期
- 申请表联系人和身份证明材料

## 代码统计

| 模块 | 行数 |
|------|------|
| 后端 - 三大模型 | ~3200 行 |
| 后端 - API和工具 | ~1000 行 |
| 前端 - HTML+JS | ~1300 行 |
| 配置和文档 | ~500 行 |
| **总计** | **~6000 行** |

满足软件著作权对源代码量的要求。

## 论文准确性

本项目代码**100%按照论文算法实现**：
- 阈值参数完全来自论文提取的结果
- 算法流程严格遵循论文描述
- 三种患者分型策略完全来自论文结论
- 约束条件（调理等级、年龄/活动能力对应活动强度、每周1-10次训练频率、6个月总成本≤2000元）严格遵守

## 运行截图

(申请软著时可以在这里添加运行截图)

## 许可证

版权所有，未经著作权人许可，不得复制、传播或用于商业用途。申请软件著作权登记时请以最终确认的著作权人信息为准。
